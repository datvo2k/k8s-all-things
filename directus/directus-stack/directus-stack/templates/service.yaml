apiVersion: v1
kind: Service
metadata:
  name: {{ include "directus-stack.fullname" . }}
  labels:
    {{- include "directus-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "directus-stack.selectorLabels" . | nindent 4 }}
